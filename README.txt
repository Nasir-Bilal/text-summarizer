# 📚 Text Summarizer Chatbot

This is a Streamlit web app that summarizes long English texts using a pretrained LLM (BART). It automatically handles long inputs by recursively chunking and summarizing them.

## Features
- Auto-handles long documents
- Streamlit interface
- Dynamic summary sizing
- Deployable to Streamlit Cloud

## How to Run Locally

```bash
pip install -r requirements.txt
streamlit run app.py
